# Server Environment Variables
PORT=3001
YTDLP_PATH=yt-dlp # Use path to binary if not in PATH

# Frontend Environment Variables (Vite)
# When deploying to Vercel, set this to your Render backend URL + /api
VITE_API_URL=https://your-backend-name.onrender.com/api
